<div class="container" style='margin-top:10px'>
     
    <div class="row justify-content-center">
    
        <div class="col-xl-10 col-lg-11">
            
            <div class="page-header">
                {# <h2 style="color:#353A40">Welcome Giallo<span style='color:#debc85;'>!</span></h2> #}
            
                
                <div class="d-flex justify-content-between">
                    <div class="d-flex align-items-center">
                    </div>
                    <div class="d-flex align-items-center">

                        {# Button : To create a new menu item #}
                        
                        <a data-toggle="collapse" class="btn btn-primary" href="#drop-file" role="button" aria-expanded="false" aria-controls="drop-file" style="margin-left:12px;">
                            Ajoute un nouveau plat
                        {# </button> #}
                        </a>
                    </div>
                </div>
            </div>
            <hr>  
            <div id="drop-file" class="collapse" aria-labelledby="drop-file">
                <div style='width:500px;margin:auto;border:1px solid rgba(0,0,0,.1);padding:25px;border-radius:0.5rem;border-top:2px solid #ffc107;'>

                    {# {{ form_start(smartUploadForm) }}
                    <div class="row">
                        <div class="col-md-6"> 
                        
                        {{ form_row(smartUploadForm.classYearStart) }}

                        </div>
                        <div class="col-md-6">
                        
                        {{ form_row(smartUploadForm.classYearEnd) }}

                        </div>
                    </div>
                    <br>
                    {{ form_row(smartUploadForm.excelFile) }}
                   
                    <br>
         
                    {{ form_row(smartUploadForm.submit) }}
               
                    
                    {{ form_end(smartUploadForm) }} #}
                    <br>
                    {# <form class="dropzone" style='color:#353A40;border-radius:0.5rem'>
                        <span class="dz-message" >Drop file here or click here to upload</span>
                    </form> #}
                    
                </div>
                
                <br>
            </div>

            
           
            
        
    

            
            
                        
                       